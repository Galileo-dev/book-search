#include <iostream>
#include "linkedlist.h"

int main()
{
    LinkedList<int> *linkedList = new LinkedList<int>();
    // tree->root = new Node<int>(5);
    // tree->root->left = new Node<int>(3);

    // std::cout << "Root Node value: " << tree->root->value << std::endl;
    // std::cout << "Left Node value: " << tree->root->left->value << std::endl;

    return 0;
}